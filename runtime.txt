git add runtime.txt
git commit -m "Force Python 3.11.0"
git push origin main
```

### 3. Alternative: Use Environment Variable (if runtime.txt doesn't work)

Go to Render → **Environment** tab:
- Add variable:
  - **Key:** `PYTHON_VERSION`
  - **Value:** `3.11.0`

Then **Manual Deploy** → **Clear build cache & deploy**

---

## Why this happened:

The log shows:
```
==> Installing Python version 3.13.4...
==> Using Python version 3.13.4 (default)
